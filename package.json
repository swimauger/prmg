{
  "name": "prmg",
  "version": "1.0.0",
  "description": "Generate a Procedural Game Map using a PRNG",
  "main": "lib/prmg.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc --watch",
    "clean": "rimraf lib"
  },
  "dependencies": {
    "@prng/core": "^1.0.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
